# Backend configuration is handled via terraform init -reconfigure
# Add your backend block here if using S3 remote state, e.g.:
#
# terraform {
#   backend "s3" {
#     bucket = "my-terraform-state"
#     key    = "async-ai-job/terraform.tfstate"
#     region = "us-east-1"
#   }
# }
