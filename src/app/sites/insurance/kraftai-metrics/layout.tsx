export const metadata = {
  title: 'Insurance Metrics | KraftAI',
  robots: { index: false, follow: false },
};

export default function MetricsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
